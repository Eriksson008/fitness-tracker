# Claude Instructions

When editing this file, update the counterpart file in the same change.

Read `PROJECT_CONTEXT.md` first.

Keep `AGENTS.md` aligned with this file. Preserve the local-first fitness tracker MVP, protect secrets and local data, use Prisma migrations for schema changes, and keep calculator formulas documented and tested.

Before committing, run:

```powershell
npm run test
npm run typecheck
npm run lint
npm run build
```
